[workspace]
members = [
    "license-core",
    "license-registry",
    "api-license",
    "content-license",
]
resolver = "2"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
